# Cú pháp

. git rebase <tên_nhánh>: Lấy các commit của nhánh hiện tại và đặt lại lên trên commit mới nhất của nhánh kia.

. git rebase --continue: Dùng khi rebase bị conflict.

. git rebase --skip: Dùng khi bỏ qua commit đang gây lỗi.