# Cách cài đặt database Northwind để test
1. Vào SQL Server tạo database mới tên Northwind
2. Mở trang query mới cho database vừa tạo
3. Mở file init và copy code để vào chạy file query để thêm dữ liệu mới
